// Використовуючи конструкцію if..else через тернарний вираз, напишіть код,
// який отримує число через prompt, а потім виводить в console.log() повідомлення:
// — Число парне.
// — Число непарне.
// — Число 0.
let number = +prompt('Укажите число');
number == 0 ? console.log('Число 0') : number % 2 ? console.log('число непарное') : console.log('число парное');
